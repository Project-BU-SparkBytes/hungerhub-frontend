export interface User {
  "id": string,
}

export interface Event {
  id: number;
  name: string;
  description: string;
  location: string;
  date: string;
  time: string;
  student_alumni_prof: string;
  food_available: string;
}